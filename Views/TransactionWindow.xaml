<Window x:Class="FinanceAnalysisSystem.Views.TransactionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить операцию" Height="400" Width="500"
        Background="#F5F5F5" WindowStartupLocation="CenterOwner" WindowStyle="ToolWindow">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Новая финансовая операция" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- Type Selection -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Тип операции:" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,10">
            <RadioButton Content="Доход" IsChecked="{Binding Type, Converter={StaticResource TypeConverter}, ConverterParameter=Income}" Margin="0,0,30,0"/>
            <RadioButton Content="Расход" IsChecked="{Binding Type, Converter={StaticResource TypeConverter}, ConverterParameter=Expense}"/>
        </StackPanel>

        <!-- Description -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Описание:" VerticalAlignment="Top" Margin="0,5,0,0"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Height="35" Padding="10" Margin="0,0,0,10" Foreground="#2C3E50" Background="White" BorderBrush="#BDC3C7"/>

        <!-- Amount -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Сумма:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}" Height="35" Padding="10" Margin="0,0,0,10" Foreground="#2C3E50" Background="White" BorderBrush="#BDC3C7"/>

        <!-- Category -->
        <TextBlock Grid.Row="4" Grid.Column="0" Text="Категория:" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="4" Grid.Column="1" ItemsSource="{Binding Categories}" SelectedItem="{Binding Category, UpdateSourceTrigger=PropertyChanged}" Height="35" Padding="10" Margin="0,0,0,10" Foreground="#2C3E50" Background="White" BorderBrush="#BDC3C7"/>

        <!-- Date -->
        <TextBlock Grid.Row="5" Grid.Column="0" Text="Дата:" VerticalAlignment="Center"/>
        <DatePicker Grid.Row="5" Grid.Column="1" SelectedDate="{Binding Date}" Height="35" Padding="10" Margin="0,0,0,10" Background="White" BorderBrush="#BDC3C7"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="7" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Height="40" Margin="0,15,0,0">
            <Button Content="✓ Добавить" Click="ConfirmButton_Click" Background="#27AE60" Foreground="White" Padding="20,0" FontWeight="Bold" Margin="0,0,10,0"/>
            <Button Content="✕ Отмена" Click="CancelButton_Click" Background="#E74C3C" Foreground="White" Padding="20,0" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>